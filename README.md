# homebrew-tap

Homebrew formulas for my tools. No `brew tap` needed — install directly:

```sh
brew install kerem-kaynak/tap/<formula>
```

| Formula | What |
| --- | --- |
| [`wt`](https://github.com/kerem-kaynak/wt) | Git worktrees that set themselves up. One shell function. |
